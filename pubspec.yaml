name: sensio_assignment
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_foreground_task: ^9.2.2
  lottie: ^3.3.3
  flutter_bloc: ^9.1.1
  flutter_reactive_ble: ^5.5.0
  bloc: ^9.2.1
  meta: ^1.17.0
  flutter_svg: ^2.3.0
  permission_handler: ^12.0.3
  fl_chart: ^1.2.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:

  uses-material-design: true

  assets:
    - "assets/animations/"
    - "assets/images/"
